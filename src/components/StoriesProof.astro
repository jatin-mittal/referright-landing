---
import SectionIntro from './SectionIntro.astro';
import TestimonialStage, { type Testimonial } from './TestimonialStage.astro';
import { SectionId } from '../lib/constants';

interface Props {
	readonly batches: readonly (readonly Testimonial[])[];
	readonly referralCount: number;
}

const { batches, referralCount } = Astro.props;
---

<section class="section stories-proof stories" id={SectionId.Stories} aria-labelledby="stories-title">
	<div class="shell">
		<SectionIntro
			eyebrow="Real people, real feedback"
			title="What people told us."
			copy="What our early community thinks about making referrals easier."
			headingId="stories-title"
			align="center"
			class="reveal"
		/>

		<div class="stories-proof-layout">
			<aside class="stories-proof-stat reveal" aria-label="Early release referral result">
				<div class="stories-proof-rings" aria-hidden="true"></div>
				<p class="stories-proof-label">Early release snapshot</p>
				<p class="stories-proof-count">
					<span data-count-to={referralCount}>{referralCount}</span>+
				</p>
				<p class="stories-proof-caption">people successfully referred during the early release</p>
			</aside>

			<div class="stories-proof-testimonials reveal" aria-label="Community feedback">
				<TestimonialStage batches={batches} />
			</div>
		</div>
	</div>
</section>

<script>
	import { countUp } from '../lib/motion';

	/* The one number on the page worth counting to. Under reduced motion, or
	   with no observer, it is simply printed. */
	const count = document.querySelector<HTMLElement>('[data-count-to]');
	if (count) countUp(count);
</script>

<style>
	.stories-proof {
		border-top: 1px solid rgb(var(--color-border));
	}

	.stories-proof-layout {
		display: grid;
		grid-template-columns: minmax(15rem, 19rem) minmax(0, 1fr);
		gap: clamp(2rem, 5vw, 5rem);
		align-items: center;
		margin-top: clamp(2.5rem, 5vw, 3.5rem);
	}

	.stories-proof-stat {
		position: relative;
		isolation: isolate;
		display: flex;
		min-height: 19rem;
		flex-direction: column;
		justify-content: center;
		overflow: hidden;
		padding: clamp(1.5rem, 3vw, 2rem);
		border: 1px solid rgb(var(--color-brand-panel-ink) / 18%);
		border-radius: var(--radius-xxl);
		background: rgb(var(--color-brand-panel));
		box-shadow: var(--shadow-panel);
		color: rgb(var(--color-brand-panel-ink));
	}

	.stories-proof-rings {
		position: absolute;
		z-index: -1;
		top: 50%;
		left: 58%;
		width: 30rem;
		aspect-ratio: 1;
		border: 1px solid rgb(var(--color-primary-strong) / 20%);
		border-radius: 50%;
		box-shadow:
			0 0 0 4.5rem rgb(var(--color-primary-strong) / 7%),
			0 0 0 9rem rgb(var(--color-primary-strong) / 6%),
			0 0 0 13.5rem rgb(var(--color-primary-strong) / 5%);
		transform: translate(-50%, -50%);
	}

	.stories-proof-label {
		margin: 0;
		color: rgb(var(--color-brand-panel-ink));
		font-family: var(--font-mono);
		font-size: 0.6875rem;
		font-weight: 600;
		letter-spacing: 0.14em;
		text-transform: uppercase;
	}

	.stories-proof-count {
		margin: 0.625rem 0 0;
		font-family: var(--font-display);
		font-variant-numeric: lining-nums tabular-nums;
		font-size: clamp(5rem, 10vw, 7rem);
		font-weight: 400;
		letter-spacing: -0.06em;
		line-height: 0.85;
	}

	.stories-proof-caption {
		max-width: 12rem;
		margin: 1.25rem 0 0;
		color: rgb(var(--color-brand-panel-ink) / 74%);
		font-size: 0.875rem;
		line-height: 1.5;
	}

	.stories-proof-testimonials {
		min-width: 0;
	}

	@media (max-width: 63.999rem) {
		.stories-proof-layout {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.stories-proof-stat {
			min-height: 15rem;
			max-width: 32rem;
			margin-inline: auto;
		}

		.stories-proof-count {
			font-size: clamp(4.5rem, 18vw, 6rem);
		}
	}
</style>
